/**The let declaration declares a block-scoped local variable,
 * optionally initializing it to a vallue
 */

{
let a = 6
console.log(a);
}

/*The var statement declases a function-scoped or globally-scoped variable,
optionally initializing it to a value.
*/

var b = 5;
console.log(5);