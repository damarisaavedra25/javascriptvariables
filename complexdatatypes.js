/**
 * Objects
 */

const obj = {
    name: 'John Doe',
    age: 33,
    isEmployed: true,
    friends: ['Mike', 'Jack'],
    address: {
        street: 'Pine',
        house: 4
    }
};

console.log(obj.age);
console.log(obj);

/**
 * Arrays
 */

const arr = [1, "string", true, [1, 2, 3], { key: "value" }];
console.log(arr.length);
