/**constants are blocked-scoped, much like variables declared using the let keyboard.
 * the value of a constant cant be changed through reassignment (i.e. by using
 * the assignment operator),
 * and it can't be redeclared (i.e. through a variable declaration)
 * 
 */
{
const a = 6;
a=7;
console.log(a);
}

//trying to reasign value